Description:
    Generate the required file(s) associated with a Filtering scope.

Example:
    bin/rails generate vog:filtering_scope Thing

    This will create:
        app/graphql/types/filtering/thing_filter_input_type.rb
